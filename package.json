{
  "name": "hardness-book-site",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "publish": "npm run build && rsync -av --no-g dist/ www/",
    "draft": "cp book.pdf public/drafts/`date -I`.pdf && ls -l public/drafts/`date -I`.pdf"
  },
  "devDependencies": {
    "astro": "5.17.2",
    "sass": "1.97.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@typopro/web-linux-libertine": "^3.7.5",
    "astro-analytics": "^2.2.0",
    "purecss": "^2.1.0"
  }
}
